@*
    Updates Page (/updates)
    Shows all locally tracked games that have a newer version available on F95zone.
    Displays via GameUpdaterComponent which lists updatable games with links to
    download pages and options to ignore updates.
*@
@page "/updates"
@inherits FluxorComponent
<GameUpdaterComponent />