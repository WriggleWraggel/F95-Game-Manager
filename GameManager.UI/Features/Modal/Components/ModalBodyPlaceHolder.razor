@*
    ModalBodyPlaceHolder
    Default empty placeholder component used as the initial ModalBody in ModalState
    before any modal is opened. Prevents null-reference errors when the modal wrapper
    attempts to render a dynamic component before a real body type is dispatched.
*@
<h3>ModalBodyPlaceHolder</h3>

@code {

}
